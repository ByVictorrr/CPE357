#include <stdio.h>

char *decompess(HuffmanEnodded results)
{
	Node *curr = 
	int i=0;
	while(i < encodedDatat.lengh())
	{
		while(!isLeaf(curr))
		{
			char bit = getEncoded data bit (I)
			if( bit == '1'){

			}else if (bit == '0'){
				curr = curr->left_child;
			}
			else{
				printf("no valid bit\n");
			}
			i++;
		}

	}
}
